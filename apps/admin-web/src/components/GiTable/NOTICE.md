GiTable is adapted from `lin-97/gi-demo` (`src/components/GiTable`), licensed under Apache-2.0.

Local changes:

- Adapted the component into ShiroAdmin admin-web.
- Added the ShiroAdmin `request` / `actionRef` / `RequestData` table contract.
- Removed the `vue-draggable-plus` runtime dependency and implemented local column ordering.
