# Cerbos Policies

部署目标目录下的 `docker/config/app-api-cerbos/policies/` 用于挂载给 App API Cerbos 实例。

这个目录只保留占位说明，避免本地部署目录再次引用不存在的路径。

如果要做冷启动恢复，请从以下任一来源同步真实策略：

- Cerbos Hub
- 目标部署目录下的 `docker/config/app-api-cerbos/policies/`
- 内部策略仓库备份
