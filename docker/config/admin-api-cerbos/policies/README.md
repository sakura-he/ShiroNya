# Admin Cerbos Policies

本目录从 `1111` 分支的 `cerbos_policies/stores/admin/policies` 同步而来，用于 `admin-api` 专用 Cerbos 实例。

服务器部署目录为 `/opt/shiro-nya/docker/admin-cerbos/policies`，当前 Docker Compose 通过只读挂载把它提供给 Cerbos 的 `disk` store。
