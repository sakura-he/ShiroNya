# Cerbos Policies

服务器上的 `/opt/shiro-nya/docker/cerbos/policies/` 当前是独立目录，但这些策略文件没有纳入当前仓库版本控制。

这个目录只保留占位说明，避免本地部署目录再次引用不存在的路径。

如果要做冷启动恢复，请从以下任一来源同步真实策略：

- Cerbos Hub
- 服务器 `/opt/shiro-nya/docker/cerbos/policies/`
- 内部策略仓库备份
